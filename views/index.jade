extends layout

block content
  h3 Today's Hot Artists
  ul(data-hot-artists)
    each artist in hotArtists
      li 
        a(href="/artists/#{urlify(artist.name)}/#{artist.id}") 
          span #{artist.name}  